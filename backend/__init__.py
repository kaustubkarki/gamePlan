# features and purpose of init(also it makes py recognize any folder with init file to mark it as a library for the program)
#It still serves the basic purpose of marking the directory as a Python package, enabling you to import modules from that directory.
# Grouping imports.
# Managing configurations.
# Exposing utility functions or classes.